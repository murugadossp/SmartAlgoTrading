# Test data

- **groww_Stocks_Holdings_Statement.xlsx** — Sample Groww holdings Excel used by `tests/test_parser_agent.py` (direct agent test). Copy from your Downloads folder if missing; the script skips Run 2 when the file is not present.
